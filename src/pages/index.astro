---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import HelpersList from '../components/helpers-list';

const helpers = await getCollection('helpers');
---

<Layout title='Home'>
  <HelpersList helpers={helpers} />
</Layout>
